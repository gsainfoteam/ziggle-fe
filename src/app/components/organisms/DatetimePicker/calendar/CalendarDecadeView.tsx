// a placeholder file, since Calendar virtually omits usage of this.

import dayjs from 'dayjs';

interface CalendarDecadeViewProps {
  value?: dayjs.Dayjs;
}

const CalendarDecadeView = ({ value }: CalendarDecadeViewProps) => {
  return undefined;
};

export default CalendarDecadeView;
